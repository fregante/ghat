env:
# Generated by `npx ghat fregante/ghat/templates/node --exclude jobs.Build --exclude jobs.Test`
# DO NOT EDIT BELOW

name: CI
on:
  - pull_request
  - push
jobs:
  Lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: >-
          if [ -z ${{ hashFiles('package-lock.json', 'npm-shrinkwrap.json') }}
          ]; then npm install; else npm ci; fi;
        name: npm install
      - run: npx xo
